[package]
name = "vigilansee-parser"
version = "0.1.0"
edition = "2024"

[dependencies]
rust-s3 = { version = "0.37.1", default-features = false, features = ["tokio-rustls-tls"] }
dotenvy = { workspace = true }

[dev-dependencies]
tokio = { version = "1.50.0", features = ["full"] }
